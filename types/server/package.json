{
  "name": "@types/server",
  "version": "1.0.0",
  "description": "Types for the server.",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "npx rimraf ./lib/ && tsc -p ./tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.1.6",
    "rimraf": "^5.0.1"
  }
}